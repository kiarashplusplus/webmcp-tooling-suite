# WebMCP Tooling Suite Environment Variables

# GitHub OAuth Worker URL (for authentication)
# Deploy the oauth worker first, then set this URL
VITE_GITHUB_OAUTH_URL=https://webmcp-oauth.YOUR-SUBDOMAIN.workers.dev/auth

# Directory API Worker URL (for feed directory)
# Deploy the directory worker first, then set this URL
VITE_DIRECTORY_API_URL=https://webmcp-directory.YOUR-SUBDOMAIN.workers.dev

# Optional: Custom CORS proxy for fetching feeds
# VITE_CORS_PROXY_URL=https://your-cors-proxy.workers.dev
